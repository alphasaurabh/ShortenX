# ShortenX (Next.js + Tailwind)

Local setup:
1. Copy `.env.local.example` -> `.env.local` and set MONGODB_URI and BASE_URL
2. npm install
3. npm run dev
4. Visit http://localhost:3000

This project uses the App Router (app/), JS only (no TypeScript).
